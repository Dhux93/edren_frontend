import { ArchiveroTable } from "./archivero";

export interface Image {
    portada: string;
    archId: number;
}
export interface ImageTable {
    id: number;
    Portada: string;
    archId: number;
}